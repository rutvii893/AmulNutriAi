services:
  - type: web
    name: amulnutriai
    runtime: python
    buildCommand: pip install -r requirements.txt && python data/seed_products.py
    startCommand: gunicorn app:app
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: SECRET_KEY
        sync: false
